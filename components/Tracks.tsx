import { View, Text } from 'react-native'
import React from 'react'

const Tracks = ({name, id, duration_ms}:) => {
  return (
    <View>
      <Text>Tracks</Text>
    </View>
  )
}

export default Tracks